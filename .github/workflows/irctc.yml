name: IRCTC Automation Booking

on:
  push:
    branches:
      - main

  pull_request:

  workflow_dispatch:
    inputs:
      USERNAME:
        required: true
        type: string
        description: "IRCTC Username"

      PASSWORD:
        required: true
        type: string
        description: "IRCTC Password"

      UPI_ID:
        required: true
        type: string
        description: "UPI ID"

jobs:
  IRCTC-Booking:
    runs-on: ubuntu-22.04

    timeout-minutes: 30

    steps:
      # -----------------------------
      # Checkout Repository
      # -----------------------------
      - name: Checkout Repository
        uses: actions/checkout@v4

      # -----------------------------
      # Setup Python
      # -----------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      # -----------------------------
      # Install Python Dependencies
      # -----------------------------
      - name: Install Python Dependencies
        run: |
          sudo timedatectl set-timezone Asia/Kolkata

          python -m pip install --upgrade pip

          pip install -r irctc-captcha-solver/requirements.txt

      # -----------------------------
      # Setup Node.js
      # -----------------------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # -----------------------------
      # Install Node Dependencies
      # -----------------------------
      - name: Install Node Dependencies
        run: |
          npm install
          npm install -g wait-on

      # -----------------------------
      # Start Captcha Solver Server
      # -----------------------------
      - name: Start Captcha Solver Server
        run: |
          python3 irctc-captcha-solver/app-server.py --host 0.0.0.0 --port 5000 &

          echo "Waiting for Flask server to start..."

          npx wait-on http://127.0.0.1:5000

          echo "Flask server started successfully."

      # -----------------------------
      # Prepare Environment Variables
      # -----------------------------
      - name: Prepare Environment Variables
        run: |
          PASSWORD="${{ inputs.PASSWORD || secrets.PASSWORD }}"
          USERNAME="${{ inputs.USERNAME || secrets.USERNAME }}"
          UPI_ID="${{ inputs.UPI_ID || secrets.UPI_ID }}"

          echo "::add-mask::$PASSWORD"
          echo "::add-mask::$USERNAME"
          echo "::add-mask::$UPI_ID"

          echo "PASSWORD=$PASSWORD" >> $GITHUB_ENV
          echo "USERNAME=$USERNAME" >> $GITHUB_ENV
          echo "UPI_ID=$UPI_ID" >> $GITHUB_ENV

      # -----------------------------
      # Run Cypress Automation
      # -----------------------------
      - name: Run IRCTC Cypress Automation
        run: |
          xvfb-run npx cypress run \
            --browser chrome \
            --record \
            --key ${{ secrets.CYPRESS_RECORD_KEY }} \
            --env UPI_ID=${{ env.UPI_ID }},USERNAME=${{ env.USERNAME }},PASSWORD=${{ env.PASSWORD }}

      # -----------------------------
      # Upload Screenshots on Failure
      # -----------------------------
      - name: Upload Cypress Screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: cypress/screenshots

      # -----------------------------
      # Upload Videos
      # -----------------------------
      - name: Upload Cypress Videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-videos
          path: cypress/videos
